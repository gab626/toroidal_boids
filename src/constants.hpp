#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

namespace bd {

const float windowWidth{1600.f}; // INLINE CONSTEXPR?
const float windowHeight{900.f};

const float minimumSpeed{1000.f};
const float maximumSpeed{2000.f};

const float shapeScale{3.f};

const float deltaT = .001f;

} // namespace bd

#endif